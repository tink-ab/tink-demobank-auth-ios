import UIKit
import SwiftUI

extension Color {
    static let background = Self(UIColor(named: "Background")!)
    static let button = Self(UIColor(named: "Button")!)
    static let buttonLabel = Self(UIColor(named: "ButtonLabel")!)
    static let leftToSpend = Self(UIColor(named: "LeftToSpend")!)
    static let tinkLight = Self(UIColor(named: "TinkLight")!)
    static let tinkDark = Self(UIColor(named: "TinkDark")!)
}
